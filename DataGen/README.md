# Data Generator

Generate mock spending data given an inital data file

## How to Run

1. Download the initial data file (not included in this repo), or create your own `data.csv` with columns labelled `Category` and `Price` and `Other`. 
1. Run `pip install -r requirements.txt`
1. `python data_gen.py`
